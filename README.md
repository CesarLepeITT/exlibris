# exlibris
# What is exlibris?
Exlibiris is a library designed to evaluate the performance of any binary classification model. It includes 10 tabular datasets.
# Depencences
* pandas>=1.0.5'
* numpy>=1.21
* scikit-learn>=0.22.2
# Instalation
For local instalation 
pip install -e .
